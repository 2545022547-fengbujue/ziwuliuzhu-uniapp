#!/bin/sh
# pre-commit 钩子：推送安全校验（防隐私泄露）
# 提交前扫描暂存区文件中的敏感内容（本机路径/用户名/会话ID/密钥），命中即阻止提交。
# 规则维护：scripts/verify-push-safety.cjs
echo "🔒 推送安全校验（pre-commit）..."
if ! node scripts/verify-push-safety.cjs staged; then
  echo "提交已阻止：暂存区含敏感内容，请按上方提示处理后重试。"
  exit 1
fi
exit 0
